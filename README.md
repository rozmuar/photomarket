# PhotoMarket - Сервис поиска и покупки фотографий по лицу

Веб-платформа для связи фотографов и клиентов с использованием технологии распознавания лиц.

## 🚀 Возможности

### Для клиентов:
- 📸 Загрузка селфи для поиска фотографий
- 🔍 Автоматический поиск фото с вашим лицом
- 🛒 Покупка фотографий в хорошем качестве
- 🗑️ Запрос на удаление нежелательных фото (152-ФЗ)

### Для фотографов:
- 📁 Создание событий и загрузка фотографий
- 🤖 Автоматическое распознавание лиц на фото
- 💰 Продажа фотографий, вывод средств
- 📊 Статистика продаж

## 🛠️ Технологии

- **Backend**: Django 4.2
- **База данных**: SQLite (разработка) / PostgreSQL (продакшен)
- **Распознавание лиц**: face_recognition / dlib
- **Платежи**: ЮКасса (YooKassa)
- **Асинхронные задачи**: Celery + Redis
- **Frontend**: Bootstrap 5, vanilla JavaScript

## 📦 Установка

### 1. Клонирование и виртуальное окружение

```bash
cd c:\OSPanel\home\photo
python -m venv venv
venv\Scripts\activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

**Примечание**: Для face_recognition на Windows может потребоваться:
- Visual Studio Build Tools
- CMake
- dlib

### 3. Настройка окружения

```bash
copy .env.example .env
# Отредактируйте .env файл
```

### 4. Миграции базы данных

```bash
python manage.py makemigrations
python manage.py migrate
```

### 5. Создание суперпользователя

```bash
python manage.py createsuperuser
```

### 6. Запуск сервера разработки

```bash
python manage.py runserver
```

Откройте http://127.0.0.1:8000/

## 📁 Структура проекта

```
photomarket/
├── apps/
│   ├── accounts/       # Пользователи, профили
│   ├── photographers/  # Кабинет фотографа
│   ├── clients/        # Кабинет клиента
│   ├── photos/         # Фотографии, события
│   ├── payments/       # Платежи, транзакции
│   └── recognition/    # Распознавание лиц
├── photomarket/        # Настройки проекта
├── templates/          # HTML шаблоны
├── static/             # CSS, JS
├── media/              # Загруженные файлы
└── requirements.txt
```

## 🗄️ Модели базы данных

### User (Пользователь)
- Расширенная модель Django User
- Тип: фотограф / клиент / оба
- Согласия на обработку данных

### PhotographerProfile (Профиль фотографа)
- Информация о студии
- Баланс и финансы
- Настройки по умолчанию

### ClientProfile (Профиль клиента)
- Селфи для распознавания
- Кодировка лица (face_encoding)
- Статистика покупок

### Event (Событие)
- Название, тип, дата
- Место проведения
- Цена по умолчанию

### Photo (Фотография)
- Оригинал, превью, с водяным знаком
- Количество лиц
- Статус, цена

### PhotoFace (Лицо на фото)
- Координаты лица
- Кодировка (embedding)
- Связь с пользователем

### Purchase (Покупка)
- Связь покупатель-фото
- Суммы и комиссия
- Токен скачивания

### DeletionRequest (Запрос на удаление)
- Фото и заявитель
- Статус рассмотрения

## 🔧 Celery задачи

```bash
# Запуск воркера
celery -A photomarket worker -l info

# Запуск планировщика (для периодических задач)
celery -A photomarket beat -l info
```

### Задачи:
- `process_photo_faces` - обработка загруженного фото
- `process_client_selfie` - обработка селфи клиента
- `match_faces_with_clients` - сопоставление лиц
- `find_client_photos` - поиск фото для клиента

## 💳 Интеграция с ЮКасса

1. Получите credentials в личном кабинете ЮКасса
2. Добавьте в `.env`:
   ```
   YOOKASSA_SHOP_ID=your-shop-id
   YOOKASSA_SECRET_KEY=your-secret-key
   ```
3. Настройте webhook на `/payments/webhook/yookassa/`

## ⚖️ Юридические аспекты (152-ФЗ)

- Обязательное согласие на обработку персональных данных
- Обязательное согласие на обработку биометрии
- Право на удаление данных
- Хранение данных на территории РФ

## 🚀 Продакшен

1. Настройте PostgreSQL
2. Настройте Redis для Celery
3. Используйте Gunicorn + Nginx
4. Настройте HTTPS
5. Храните медиа в S3-совместимом хранилище

## 📝 Лицензия

MIT
